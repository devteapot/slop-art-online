[package]
name = "living-authority"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { workspace = true }
log = "0.4"
serde = { workspace = true }
serde_json = { workspace = true }
living-rules = { path = "../rules" }
