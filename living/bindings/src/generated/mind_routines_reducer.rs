// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::routine_in_type::RoutineIn;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MindRoutinesArgs {
    pub actor: u32,
    pub routines: Vec<RoutineIn>,
}

impl From<MindRoutinesArgs> for super::Reducer {
    fn from(args: MindRoutinesArgs) -> Self {
        Self::MindRoutines {
            actor: args.actor,
            routines: args.routines,
        }
    }
}

impl __sdk::InModule for MindRoutinesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `mind_routines`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait mind_routines {
    /// Request that the remote module invoke the reducer `mind_routines` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`mind_routines:mind_routines_then`] to run a callback after the reducer completes.
    fn mind_routines(&self, actor: u32, routines: Vec<RoutineIn>) -> __sdk::Result<()> {
        self.mind_routines_then(actor, routines, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `mind_routines` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn mind_routines_then(
        &self,
        actor: u32,
        routines: Vec<RoutineIn>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl mind_routines for super::RemoteReducers {
    fn mind_routines_then(
        &self,
        actor: u32,
        routines: Vec<RoutineIn>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(MindRoutinesArgs { actor, routines }, callback)
    }
}
