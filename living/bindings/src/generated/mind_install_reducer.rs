// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::thought_in_type::ThoughtIn;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MindInstallArgs {
    pub actor: u32,
    pub graph: String,
    pub plan: String,
    pub say: String,
    pub say_to: u32,
    pub seen_ms: u64,
    pub thought: ThoughtIn,
}

impl From<MindInstallArgs> for super::Reducer {
    fn from(args: MindInstallArgs) -> Self {
        Self::MindInstall {
            actor: args.actor,
            graph: args.graph,
            plan: args.plan,
            say: args.say,
            say_to: args.say_to,
            seen_ms: args.seen_ms,
            thought: args.thought,
        }
    }
}

impl __sdk::InModule for MindInstallArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `mind_install`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait mind_install {
    /// Request that the remote module invoke the reducer `mind_install` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`mind_install:mind_install_then`] to run a callback after the reducer completes.
    fn mind_install(
        &self,
        actor: u32,
        graph: String,
        plan: String,
        say: String,
        say_to: u32,
        seen_ms: u64,
        thought: ThoughtIn,
    ) -> __sdk::Result<()> {
        self.mind_install_then(actor, graph, plan, say, say_to, seen_ms, thought, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `mind_install` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn mind_install_then(
        &self,
        actor: u32,
        graph: String,
        plan: String,
        say: String,
        say_to: u32,
        seen_ms: u64,
        thought: ThoughtIn,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl mind_install for super::RemoteReducers {
    fn mind_install_then(
        &self,
        actor: u32,
        graph: String,
        plan: String,
        say: String,
        say_to: u32,
        seen_ms: u64,
        thought: ThoughtIn,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            MindInstallArgs {
                actor,
                graph,
                plan,
                say,
                say_to,
                seen_ms,
                thought,
            },
            callback,
        )
    }
}
