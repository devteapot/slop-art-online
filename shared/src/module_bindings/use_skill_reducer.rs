// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UseSkillArgs {
    pub skill_id: u64,
    pub target_x: f32,
    pub target_y: f32,
    pub target_z: f32,
}

impl From<UseSkillArgs> for super::Reducer {
    fn from(args: UseSkillArgs) -> Self {
        Self::UseSkill {
            skill_id: args.skill_id,
            target_x: args.target_x,
            target_y: args.target_y,
            target_z: args.target_z,
        }
    }
}

impl __sdk::InModule for UseSkillArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `use_skill`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait use_skill {
    /// Request that the remote module invoke the reducer `use_skill` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`use_skill:use_skill_then`] to run a callback after the reducer completes.
    fn use_skill(
        &self,
        skill_id: u64,
        target_x: f32,
        target_y: f32,
        target_z: f32,
    ) -> __sdk::Result<()> {
        self.use_skill_then(skill_id, target_x, target_y, target_z, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `use_skill` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn use_skill_then(
        &self,
        skill_id: u64,
        target_x: f32,
        target_y: f32,
        target_z: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl use_skill for super::RemoteReducers {
    fn use_skill_then(
        &self,
        skill_id: u64,
        target_x: f32,
        target_y: f32,
        target_z: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UseSkillArgs {
                skill_id,
                target_x,
                target_y,
                target_z,
            },
            callback,
        )
    }
}
