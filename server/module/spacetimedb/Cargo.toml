[package]
name = "server_module"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["cdylib"]

[features]
# Explicit diagnostic builds only: timers write module logs and perturb timings.
clock-profile = []

[dependencies]
spacetimedb = { version = "=2.1.0", features = ["unstable"] }
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
bonsai-bt = { version = "0.11", features = ["serde"] }

simulation = { path = "../../../simulation" }
