[package]
name    = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["bevy_dev_tools"] }
spacetimedb-sdk = { version = "=2.0.1" }
shared = { path = "../shared" }
bevy_voxel_world = "0.15"
