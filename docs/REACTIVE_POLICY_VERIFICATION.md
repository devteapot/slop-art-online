# Reactive policy verification — 2026-09-05

The persistent reactive policy implementation passes deterministic and real-database checks. **An actual local LLM-generated policy was installed and executed unchanged, but adaptive conditional branch execution remains unverified.** The initial Luna run expired without a response and the slower hosted follow-up returned HTTP 524. Three explicitly authorized local generations yielded two rejected proposals and one accepted tree whose first branch monopolized execution until death. The later two calls followed a focused contract fix and recorded semantic feedback; all raw outputs and rejections are retained. The actual LLM contract requests each individual's own conditional tree; the runtime does not substitute a fixture or authored hazard policy when no output arrives.

Later endpoint work: [Carlid streaming integration](CARLID_STREAMING_VERIFICATION.md) preserves this historical policy evidence and documents the subsequent transport/capture fixes and one incomplete hosted check.

## Implementation checks

`cargo test -p simulation -p bridge --lib --bins` passed **45 tests**: 25 authoritative-core tests (including eleven new policy cases), 19 reasoning/provider tests and one old-manifest archive compatibility test. WASM module and native runner builds passed. Existing legacy unused-code/import warnings remain.

The new cases use the real core and prove newly perceived danger causes an installed tree to branch without a second model response; running action progress continues while inference is pending; damage retains policy and generation; explicit conditions prevent unintended return to known danger; priority preemption interrupts the running action and resets the abandoned sequence; uninterrupted cursors and event sequences survive serialize/restore; conditions cannot query hidden sites; latest subjective food observations win; human/AI policy effects match; death/controller/replacement invalidate requests; delayed reflections cannot replace newer danger beliefs; invalid/deep/wide/oversized trees do not replace an installed policy; and failure does not revert to authored survival behavior. The legacy authored-navigation test was intentionally replaced by a test that bootstrap does not invent a navigation policy.

The current model parser requires `PolicyProposal`; the authority separately retains legacy action-list inputs. Existing native Ollama, specialized OpenRouter and generic endpoint tests pass with the new schema/typed proposal. No old archive file was migrated or rewritten. New native comparison loaded old M1, first hosted and new hosted archives successfully.

## Real reducer proof — deterministic fixture

[Reactive integration report](../output/reactive-verification-1788566777009085000/report.json), database `sim-reactive-1788566777009159000`, and [retained snapshot](../output/reactive-verification-1788566777009085000/snapshot.json) were generated by [verify_reactive.py](../scripts/verify_reactive.py). Each step uses the real SpacetimeDB reducers, which reload persisted execution JSON. The companion [legacy regression report](../output/m1-verification-1788566783432032000/report.json) passes isolation, owner/private-table boundaries, controller parity, old sequence semantics, speech/reflection, mortality and causal checks in fresh isolated databases.

The [fixture policy](../output/reactive-verification-1788566777009085000/fixture-policy.json) is explicitly test-authored, **not LLM-generated**. Its priority branches are: retreat to location 0 when current-position danger is believed; otherwise explore while fixed location 1 is not believed dangerous; otherwise reconsider and rest. This policy represents an input the model is permitted to propose, not an automatic runtime template.

The [read-only fixture inspector](http://127.0.0.1:18883) shows 10 ticks and 168 events. Select Tovan (human) to see the uninterrupted fixture tree at decision **22**, active path `root/2/1`, sequence cursor `root/2: 1`, and selected priority branch 2. AI and human execute the same tree/effects. For the AI, decision **19** starts exploration; damage **35** at tick 2 interrupts only its running action (**38–39**) and creates pending request **41**. At tick 3, guard **59** sees the newly learned danger, move **64** retreats, and branch selection **66** changes 1 → 0. At tick 4, branch **83** selects recovery. It remains away from danger and gains energy without another model result until the explicit fixture revision at tick 10. Human branch **76** shows the same 1 → 0 switch. The late AI fixture request remains acceptable after harm; invalid human tree input is rejected without replacing the human's installed tree.

Browser verification confirmed the tree outline, active path, persistent sequence cursor, branch selection and causal-parent detail; no console errors were observed. The fixture is an archived observation, with operator actions disabled. The database is not advancing.

## One bounded hosted attempt

Command:

```bash
python3 scripts/run_carlid_npc.py output/carlid-luna-reactive --port 18882
```

The saved credential was loaded only by the existing safe launcher. Requested model `gpt-5.6-luna`, generic endpoint `https://codex.carlid.dev/v1`, prompt-only JSON, requested `max_completion_tokens: 6000`, one attempt per request, 90-second wall deadline, nominal two-second tick delay and original 45-tick survival scenario. The proxy's inspected source drops output-limit fields; see [the source finding](PROXY_TOKEN_LIMIT_FINDING.md). Neither model output capacity nor cost was silently capped by an assumed 1,200-token limit.

The [hosted archive](../output/carlid-luna-reactive/manifest.json), run **sim-1788566809925-40583**, ended at **45 ticks, 662 events**. Its [read-only inspector](http://127.0.0.1:18882) and [machine verification](../output/carlid-luna-reactive/verification.json) retain all four attempts:

| Requests | Result |
|---|---|
| 19, 27, at tick 1 | No complete response by logical expiry; local waits cancelled after approximately 79.5 seconds. |
| 455, 463, at tick 33 | No complete response by run end; local waits cancelled after approximately 33.5 seconds. |

No policy was installed; **there are no live generated conditional trees or branches to claim from this run**. All actual provider responses, served-model identities, token usage and cost are unavailable/unknown. A cancelled client wait does not prove upstream generation or charging stopped. Synthetic expiry-result audit records are distinguishable from the journaled actual HTTP attempts.

All four dispatched user messages equal the matching frozen subjective contexts and include the new policy contract. Every journal contains start/finish/completion records, and API bytes match saved files. Snapshot API equality and ordered causal references passed. Both AI characters remained at their starting locations with health 100/hunger 97 under labeled bootstrap; the unattended human died of starvation. This is not evidence of better survival or model reasoning. Unlike the first hosted run's accepted sequences, this run provides no returned behavior to compare. The pending-request limit, wall-time and scenario-duration combination prevented live verification; there is no evidence that damage invalidation caused these cancellations.

## Diagnosed hosted follow-up: HTTP 524, not bad model output

One slower follow-up was explicitly authorized after the initial coordination cap. It used the same endpoint/model, 45-tick scenario and single attempt per request, with `SIM_TICK_MS=8000` and the separate [240-second config](../configs/reasoning/codex-carlid-luna-reactive-slow.json):

```bash
SIM_TICK_MS=8000 python3 scripts/run_carlid_npc.py output/carlid-luna-reactive-slow \
  --config configs/reasoning/codex-carlid-luna-reactive-slow.json --port 18884
```

Run **sim-1788567278004-42046**, [archive](../output/carlid-luna-reactive-slow/manifest.json), [verification](../output/carlid-luna-reactive-slow/verification.json), and [read-only inspector](http://127.0.0.1:18884):

- Requests **19/27** returned HTTP **524** after about **125.2 seconds**, with the exact non-JSON body `error code: 524\n`. This happened before the configured wall deadline or logical expiry. No model text or served identity was returned. It is an upstream HTTP failure, not evidence that generated policy JSON failed validation.
- The runner had automatically started new pending requests **235/243** at tick 17 before the failure was diagnosed. Those waits were subsequently cancelled after about **76.5 seconds**, with no response or usage available. Every request still had one attempt; no retry or model switch occurred.
- Further dispatch was stopped by pausing this runner. The real `sim_step` reducer was then called to reach the configured 45-tick stop, and the runner resumed only to cancel/drain existing waits and export. [Operator termination](../output/carlid-luna-reactive-slow/operator-termination.json) records this intervention. The final 45 ticks/658 events must **not** be described as a full eight-second-paced experiment; the ending segment was advanced without wall delay. No generated or fixture policy was injected.

All four contexts and journal API bytes match the retained records, and causal/snapshot checks pass. No policy was installed. Token usage, cost and served model remain unknown for every request. The source's buffering of non-streaming Chat Completions until upstream completion is consistent with this gateway failure, but the exact deployed failing hop was not independently identified. The proxy was not changed.

The original audit preserves its then-current `malformed provider JSON` classification and secondary empty-raw `invalid model decision: EOF...`. A post-run local fix now reports **HTTP 524 non-JSON provider failure**, and the authority labels an empty failed reasoning outcome **no proposal returned** rather than malformed generated policy. Added provider/core tests and a [final real-reducer check](../output/reactive-verification-1788567645811918000/report.json) pass. No external request was made to test the classification fix.

## One local generation, honestly rejected

A single local generation was separately authorized to test the actual generated contract without further hosted requests. [verify_local_generated_policy.py](../scripts/verify_local_generated_policy.py) exported a pending request from a fresh real database, invoked the existing Reasoner once through [local_policy_probe.rs](../server/bridge/examples/local_policy_probe.rs), submitted the output unchanged, and advanced real reducers without any further model responses.

[Local archive](../output/local-reactive-proof/manifest.json), [raw generation](../output/local-reactive-proof/generation.json), [verification](../output/local-reactive-proof/verification.json), [read-only inspector](http://127.0.0.1:18885): run **sim-local-reactive-1788567830097999000**, 30 ticks/277 events. The small scenario uses one AI, two unattended humans and one initial food at location 0. Logical time was held at tick 1 during generation, then 29 direct reducer steps ran. This isolates a one-generation contract check, not real-time concurrency or Luna compatibility.

Ollama reported **qwen2.5:7b**, **2,140 prompt tokens**, **128 completion tokens**, and about **10.3 seconds**. The actual generated tree was:

```json
{
  "kind": "reconsider",
  "reason": "Evaluate initial beliefs and set a course towards the eastern clearing for food."
}
```

The complete returned proposal included a reported explanation and reflection, but **no skill action**. The authority rejected result **27** at event **29** with `policy needs at least one skill action`; it installed no tree and applied no reflection. No branch executed. The raw response was not edited, no action or guard was appended, and no repeat prompting/tuning was performed. Context, journal API, snapshot and causal checks passed. This is actual local model output evidence and successful invalid-policy rejection, **not** successful generated reactive behavior. Cost/cache usage were not reported.

## Two targeted local follow-ups after contract correction

The initial root-only `reconsider` exposed a schema/usability mismatch. The model-facing root now excludes that intrinsically invalid form while recursive nodes retain it as a control leaf. The prompt foregrounds executable skill content and model-chosen runtime conditions/alternatives. A regression checks both root exclusion and nested availability. This supplies no authored strategy. The skill descriptions also clarify that move needs a non-null destination and gather acts at the current position with null destination.

Two additional local generations were explicitly authorized. Each used the same `qwen2.5:7b` configuration and the same one-AI/two-human scenario, a fresh real database, logical tick 1 held during generation, one unedited submission and 29 subsequent reducer steps. No hosted call occurred and no automatic semantic retry was added.

| Evidence | Actual output and outcome |
|---|---|
| [Contract-only generation](../output/local-reactive-contract-proof/generation.json), [verification](../output/local-reactive-contract-proof/verification.json), [developer inspector](http://127.0.0.1:18886) | Run `sim-local-reactive-1788568451147334000`, 30 ticks/277 events, 2,372 prompt/259 completion tokens, 11,685 ms. A priority with an unguarded move and a guarded gather. Move omitted destination; gather incorrectly supplied one. Authority rejected result 27 at event 29: `invalid destination`. No tree or reflection installed. |
| [Feedback generation](../output/local-reactive-feedback-proof/generation.json), [verification](../output/local-reactive-feedback-proof/verification.json), [developer inspector](http://127.0.0.1:18887) | Run `sim-local-reactive-1788568559427776000`, 30 ticks/233 events, 2,906 prompt/259 completion tokens, 12,175 ms. Corrected the two skill arguments, preserving its own priority structure. Accepted decision 29 and policy installation 31 at tick 1; ten policy ticks executed. |

The final call received the previous unedited output, exact authority rejection and an explicit argument-contract review in a [retained feedback record](../output/local-reactive-contract-proof/validation-feedback.json). The complete feedback is an additional journaled message; the frozen subjective-context message is unchanged. The reasoning API accepts explicit feedback only when supplied by a caller; the ordinary runner supplies none. A mock test verifies one request, unchanged subjective context/raw output and persisted feedback. Feedback does not contain hidden scenario facts or a substitute policy.

The accepted model-generated tree chooses `priority(move to 2, guard(carried food >= 1, gather))`. The first move is unguarded, so its running/success result continually wins priority selection, including when already at location 2. The model included a conditional alternative, but it was never reached: **zero guard evaluations and zero branch switches**. Event **39** is initial branch-0 selection, not adaptation. Movement completed at **47** (tick 3), damage **51** created danger memory and pending request **56**; the same policy continued through damage while that request had no model response. Further damage ended in death **177** at tick **11**. The installed tree remains inspectable after death. This is actual generated-policy installation/execution/persistence evidence, **not successful reactive recovery or improved survival**. No generated tree was edited, no runtime semantics were bent to force another branch, and no further inference was made.

Both follow-ups pass frozen-context equality, complete journal/API equality, snapshot/API equality and ordered causal-reference checks. Cost/cache counts were not reported. All three local outputs and both hosted archives remain intact. No live simulation or inference is running; the linked inspectors only read archives.

## Native client requirement

The user clarified that observing and participating as a human player means **inside the existing Bevy game client**. The browser inspectors above are developer trace/experiment tools only; their human-intent controls do not fulfill that product requirement. Native foundation observation/participation remains open. The [current-state integration outline](CURRENT_STATE.md#native-client-integration-remains-open) and [roadmap](TODO.md#next-bounded-slice--native-foundation-client) specify the next slice: authenticated run-scoped read projections/actor ownership, one-run native rendering/observer selection, and native human skill/speech controls against the same authority. Existing client networking subscribes to legacy tables; private foundation state and operator-only mutation must be addressed explicitly. No broad Bevy integration was started in this policy task.

## Versions, limits and next decision

The two hosted attempts record rules **m1-4**, decision contract **survivor-policy-v2**, reasoning service **npc-reasoning-v3**, runner SHA-256 `826f1dea9b18f0afeda3144cb9db359c7731d21a31d866f7ffcde53e08c5f653` and module SHA-256 `d223efa38a809f5f9e7c115dedf93981327e5255f9958b30eb542939c4bb910f`. [Final comparison output](../output/reactive-comparison-final.json) loads old/new archives without changing them.

The architecture is implemented and verified with deterministic fixtures through the real reducers. Actual generated-policy installation and skill execution are now demonstrated locally. The remaining acceptance gap is a model-generated conditional tree actually changing branch in response to new subjective state. The hosted route is blocked by HTTP 524, and the accepted local tree has an unreachable guarded alternative because an earlier unguarded action keeps succeeding. All authorized checks are exhausted; no further inference is running. A future focused experiment must address policy-control-flow quality or the hosted transport limit, without substituting authored behavior for LLM output.

The final rebuild includes the error classification fix and larger 6,000-token defaults/16,384 native context. [Final test output](../output/reactive-contract-final-tests.txt) records all 45 passing tests, including explicit-feedback evidence. Final native SHA-256 is `29e07a6b029da3d7af68df1662ac8c1f2fbbf40a59184ad9af0c2c91a6e1abc4`; final module SHA-256 is `a4a6ff9ef8d92e8c87fe62b6db99f7da4d5253cbb901e290bc859e7bf272ce68`. Each experiment retains its own actual executable hashes; the hosted attempts predate the error-label fix. No hosted rerun was needed for that mock/core-tested change.

Remaining runtime limits are explicit: preempted branches restart; the root repeats terminal cycles; model-chosen guards can still be poor; conditions depend on limited and possibly wrong memory; fixed tick/request budgets apply; old-world mutation across rule versions is refused; and no general replay/migration engine or legacy Bevy behavior migration was added. Read [policy semantics](REACTIVE_POLICIES.md).

All changes remain uncommitted. No proxy modification/restart/deployment, ordinary database reset, old-archive rewrite, push or merge occurred. Preserve complete ignored output directories and journals. All linked inspectors are read-only and no simulation is advancing.
